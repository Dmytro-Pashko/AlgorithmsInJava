package week2;

import edu.princeton.cs.algs4.StdIn;
import edu.princeton.cs.algs4.StdOut;

public class Permutation {
    public static void main(String[] args) {
        final int num = Integer.parseInt(args[0]);

//        final Deque<String> elems = new Deque<>();
        while (!StdIn.isEmpty()) {
            final String item = StdIn.readString();
//            elems.addFirst(item);
        }
        for (int i = 0; i < num; i++) {
//            StdOut.println(elems.removeLast());
        }
    }
}
